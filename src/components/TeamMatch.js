import React, { Component } from 'react'


// Basically the facility should do a GET request of all teams with their facilityId and filter the array of the teams by various ageGroups.  then find a way for the manager to make X number of games between team1 and team2


class TeamMatch extends Component {
    render() {
        return(
            <React.Fragment>
                <h1>Team Match working page</h1>
            </React.Fragment>
        )
    }
}